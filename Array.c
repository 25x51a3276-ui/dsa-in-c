#include <stdio.h>

int main() {
    // Declaring and initializing an array
    int numbers[] = {10, 20, 30, 40, 50};
    
    printf("First element in the array: %d\n", numbers[0]);
    
    return 0;
}

